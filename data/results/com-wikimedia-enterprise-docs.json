{
  "schemaVersion": 1,
  "scanner": {
    "version": "1.6.0",
    "rules": "402995650b68"
  },
  "target": {
    "kind": "url",
    "ref": "https://enterprise.wikimedia.com/mcp"
  },
  "scannedAt": "2026-09-17T09:34:27.022Z",
  "durationMs": 566,
  "layers": {
    "metadata": true,
    "static": false
  },
  "tools": [
    {
      "name": "search",
      "descriptionHash": "ed3a0392a322ad852e14494fc9610df13b5d4e950ce37bc73766be55beee1178",
      "description": "Full-text search over the Wikimedia Enterprise API documentation: guide sections, endpoints and response models. Exact tokens work best (a field name such as `reference_risk_score`, a status code such as `429`, or a path such as `/v2/snapshots/{identifier}/download`). Returns ids to pass to `fetch`.",
      "labels": [
        "private-data"
      ]
    },
    {
      "name": "fetch",
      "descriptionHash": "1479d25d44833d97abb2e0f5e67464e532f698d7ed897d7f004665b017ab2f9f",
      "description": "Full text for one id from `search`: `docs/<slug>` (a guide, as markdown; a `#section` suffix is accepted), `op/<operationId>` (one endpoint rendered from the OpenAPI definition) or `schema/<name>` (one response model)."
    },
    {
      "name": "list_endpoints",
      "descriptionHash": "d92c8af9fbae563bcf7eb211af33cd469f1a1e82c8cbc20faef8bb868d623151",
      "description": "Every endpoint in the Wikimedia Enterprise OpenAPI definition, in reference order, with its technical title, its plain-language line and a link into the reference. Filter with a tag: authentication, codes, languages, projects, namespaces, batches, wikidata-batches, snapshots, wikidata-snapshots, str"
    },
    {
      "name": "get_endpoint",
      "descriptionHash": "a39184b2a1b6a3adcb09073025ef7f62696693808783389ca248ba6da6a1e312",
      "description": "The exact request and response shape of one endpoint, dereferenced from the OpenAPI definition (the source of truth), as structured JSON plus a markdown rendering. Identify it by `operationId`, or by `method` and `path`."
    }
  ],
  "findings": [
    {
      "id": "MCP-SHADOW-001",
      "severity": "medium",
      "category": "shadowing",
      "title": "Generic tool name can shadow trusted tools",
      "detail": "Tool \"search\" uses a generic high-value name. When multiple servers load together, this can intercept calls meant for a trusted server.",
      "evidence": {
        "source": "tool:search",
        "snippet": "search"
      },
      "confidence": 0.4
    },
    {
      "id": "MCP-SHADOW-001",
      "severity": "medium",
      "category": "shadowing",
      "title": "Generic tool name can shadow trusted tools",
      "detail": "Tool \"fetch\" uses a generic high-value name. When multiple servers load together, this can intercept calls meant for a trusted server.",
      "evidence": {
        "source": "tool:fetch",
        "snippet": "fetch"
      },
      "confidence": 0.4
    }
  ],
  "score": 88,
  "scoreBreakdown": {
    "shadowing": 12
  },
  "errors": []
}